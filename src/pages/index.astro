---
// English home page (route: "/"). Thin shell — composition lives in Home.astro
// (shared with /pt/), which ships both audience programs in one static page.
import BaseLayout from '../layouts/BaseLayout.astro';
import Home from '../components/Home.astro';
---

<BaseLayout>
  <Home />
</BaseLayout>
